name: build

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    if: (!contains(github.event.head_commit.message, 'chore(release)'))
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Enable Corepack (provisions Yarn from packageManager field)
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build and test
        run: yarn ci
